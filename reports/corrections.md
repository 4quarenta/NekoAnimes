# Correções de baixo risco após a auditoria

Todas as mudanças abaixo foram feitas após identificar a causa. Não foram alteradas monetização, autenticação, regras de negócio, contratos remotos nem política jurídica. Nenhuma regra de lint foi suprimida.

| Arquivo | Problema e causa | Alteração e justificativa | Teste relacionado / resultado |
| --- | --- | --- | --- |
| `apps/android/app/src/main/java/com/nekoanimes/app/MainActivity.kt` | Lint `ContextCastToActivity`: `LocalContext` nem sempre é `Activity` | Usar `LocalActivity`, que expressa a dependência de activity no Compose; manter cast específico requerido pelo app | `:app:lintPlayRelease`: erro removido; build final em `android-audit-final-build.log` |
| `apps/android/app/src/main/java/com/nekoanimes/app/player/NekoPlayerScreen.kt` | 29 erros `UnsafeOptInUsageError` no Media3; a tentativa com `kotlin.OptIn` não atende ao lint AndroidX | Adotar `androidx.annotation.OptIn` para `UnstableApi` nos pontos de uso; não suprimir o diagnóstico | `:app:lintPlayRelease`: 0 erros, 48 avisos, 2 hints |
| `apps/web/src/components/AppScreen.tsx` | Pôster com origem e proxy quebrados ficava em ciclo de fallback | Limitar a duas tentativas e exibir placeholder; evita requisições repetidas e imagem vazia | `npm run build:web`, `npm run typecheck`, `npm run test:web`: passaram; teste cobre pôster inválido |
| `apps/web/src/lib/api.ts` | Fetch direto do AniList sem prazo máximo podia aguardar indefinidamente | `AbortSignal.timeout(30000)` em solicitação já existente; falha passa ao tratamento atual | `npm run build:web`, `npm run typecheck`, `npm run test:web`: passaram; timeout real de 30 s **NÃO TESTADO** |
| `apps/web/src/screens/CategoriesPage.tsx` | Query `servers` não usada gerava requisição adicional | Remover a query/import não consumidos, sem alterar dados exibidos | `npm run build:web`, `npm run typecheck`, `npm run test:web`: passaram; categorias/paginação cobertas |
| `tools/web-smoke.mjs` | Não havia assertiva para imagem quebrada | Inserir fixture de imagem inválida e verificar placeholder | `npm run test:web`: passou |
| `.github/workflows/android-quality-gate.yml` | Sem gate separado para lint/testes/bundle em PR Android | Criar gate manual/PR com Firebase fornecido por secret, build, bundletool com SHA e relatórios; não publica | Parsing YAML local passou; execução no GitHub Actions **NÃO TESTADO** |
| `.github/workflows/android-ci.yml`, `android-staging.yml`, `android-release.yml` | Google Services obrigatório e gate de release inconsistentes | Tornar ausência de configuração Firebase erro claro e incluir lint/testes/bundletool antes de release | Parsing YAML local passou; execução real e estado dos secrets **NÃO TESTADO** |
| `apps/android/app/src/main/java/com/nekoanimes/app/web/WebViewHost.kt`, `BloggerVideoResolver.kt` | Renderer WebView encerrado podia deixar a tela sem recuperação e gerar falha sob pressão de memória | Tratar `onRenderProcessGone`, destruir a instância inválida e recriar a WebView principal usando a última URL recuperável; proteger `didCrash()` para API 24–25 | `:app:compilePlayReleaseKotlin :app:lintPlayRelease`: passou; teste físico **NÃO TESTADO** |
| `apps/android/app/build.gradle.kts` | Play Release apontava para domínios de produção sem DNS durante a fase gratuita de staging | Defaults de release apontam para Pages/Worker staging isolados; produção continua possível por `-Pneko*` e não foi declarada pronta | `:app:bundlePlayRelease`, bundletool validate e smoke público: passaram |
| `apps/android/app/src/main/AndroidManifest.xml` | Firebase trazia capacidades transitivas de Advertising ID embora não haja anúncios/atribuição | Desativar coleta via metadata e remover `AD_ID`/Ad Services do manifesto mesclado final | Manifesto mesclado Play não contém essas permissões; lint, AAB e bundletool: passaram |
| `apps/web/src/screens/PrivacyPage.tsx`, `ReportPage.tsx` | Não havia orientação pública clara para agregação e pedidos de remoção | Publicar política com contato e fluxo de solicitação de remoção que recebe provedor/URL/justificativa; não promete autorização legal | Build/typecheck/smoke web e verificação pública no Pages: passaram |
| `apps/api-worker/migrations/0006_reports_retention.sql`, `apps/api-worker/src/index.ts`, `apps/web/src/screens/PrivacyPage.tsx` | Reports resolvidos/descartados não tinham prazo automático documentado nem data de encerramento | Adicionar `closed_at`, reabrir limpando a data, remover automaticamente após 90 dias por cron diário e alinhar política/admin/Data Safety | `@neko/api-worker` typecheck; 35 testes; D1 remoto com coluna/índice verificados; Worker e Pages publicados; endpoints `/health`, `/v1/app-manifest` e política HTTP 200 |

O `package-lock.json` não versionado preexistente foi preservado. Após as correções, build Android, lint, testes unitários e bundletool passaram; AAB continua não assinado. Relatórios e logs não foram incluídos no AAB final. Nenhuma publicação/deploy na Play Store foi realizada.
